summary: "Import into Entity md_Columns from metadatabase"
description: "This is a simple API which imports into entity md_COLUMNS in metadatabase"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "body"
  name: "body"
  description: "to-do description"
  required: true
  schema:
    type: "object"
    properties:
      siunit: 
          type: "string"
          example: "K"
responses:
  200: 
    description: "OK"
  405:
    description: "Invalid input"
    