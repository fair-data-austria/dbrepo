#!/bin/bash
KEY_STORE_LOCATION="./rest-service/src/main/resources/ssl/dbrepo.p12"
KEY_STORE_ALIAS="dbrepo"
KEY_STORE_PASS="dbrepo"
CERT_ALIAS="auth"

# REQUEST
certbot certonly --standalone --preferred-challenges http -d dbrepo.ossdip.at \
		-m martin.weise@tuwien.ac.at --agree-tos

# CONVERT PEM TO DER
openssl x509 -outform der -in /etc/letsencrypt/live/dbrepo.ossdip.at/privkey.pem \
  -out /etc/letsencrypt/live/dbrepo.ossdip.at/privkey.der

# IMPORT
keytool -import -alias "${CERT_ALIAS}" -keystore "${KEY_STORE_LOCATION}" -storepass "${KEY_STORE_PASS}" \
  -file /etc/letsencrypt/live/dbrepo.ossdip.at/privkey.der